import SignUp from '../../components/SignUp/SignUp';
import { connect } from 'react-redux';

const mapReduxStateToReactProps = ({}) => {
   return {};
};

export default connect(mapReduxStateToReactProps)(SignUp);
