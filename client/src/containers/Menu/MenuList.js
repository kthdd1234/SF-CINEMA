import MenuList from '../../components/Menu/MenuList';
import { connect } from 'react-redux';

const mapReduxStateToReactProps = ({}) => {
   return {};
};

export default connect(mapReduxStateToReactProps)(MenuList);
