import SearchList from '../../components/Search/SearchList';
import { connect } from 'react-redux';

const mapReduxStateToReactProps = () => {
   return {};
};

export default connect(mapReduxStateToReactProps)(SearchList);
